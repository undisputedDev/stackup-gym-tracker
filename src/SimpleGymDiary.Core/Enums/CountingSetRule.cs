namespace SimpleGymDiary.Core.Enums;

/// <summary>Which set's rep count decides the up/down/keep marking.</summary>
public enum CountingSetRule
{
    FirstSet = 0,
    LastSet = 1,
    BestSet = 2,
}
